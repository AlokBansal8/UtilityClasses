language: android

jdk:
  - oraclejdk8

android:
  components:
    - tools
    - platform-tools
    - android-24
    - build-tools-24.0.2
    - extra-android-m2repository

# cache between builds
cache:
  directories:
  - $HOME/.m2
  - $HOME/.gradle